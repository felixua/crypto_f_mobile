import 'package:flutter/material.dart';

class AddNewCoinScreen extends StatelessWidget {
  const AddNewCoinScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
